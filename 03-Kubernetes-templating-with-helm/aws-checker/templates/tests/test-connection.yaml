apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-checker.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "aws-checker.name" . }}
    helm.sh/chart: {{ include "aws-checker.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aws-checker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
